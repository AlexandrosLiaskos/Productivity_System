/** @module app/js/modules/filters — now handled entirely by search tags */

/**
 * Initialize filters (no-op — filtering is now search-based).
 * @returns {void}
 */
export function initFilters() {
  // Filtering is handled by the search module via tag queries.
  // This module is kept for API compatibility with app.js.
}
